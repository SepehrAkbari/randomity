## Distribution

This directory contains the latest distributed version of the `randomity` package. The distribution is available on Python Package Index (PyPI) and can be installed using pip.

```bash
pip install randomity
```